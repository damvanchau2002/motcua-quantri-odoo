<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Restrict only the Configuration tab inside Discuss -->
        <record id="mail.menu_configuration" model="ir.ui.menu">
            <field name="groups_id" eval="[(5, 0, 0), (4, ref('base.group_system')), (4, ref('student_request.group_menu_configuration'))]"/>
        </record>
    </data>
</odoo>